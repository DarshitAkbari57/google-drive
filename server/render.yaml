services:
  - type: web
    name: file-uploader-app
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    plan: starter
    envVars:
      - key: MONGO_URI
        value: mongo_url
      - key: JWT_SECRET
        value: your_super_secret_key
    disk:
      name: uploads
      mountPath: /uploads
      sizeGB: 1
